#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 * A c program that prints line of text with printf
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
